const { MainNews } = require("./mainnews");
const { AlertsNews } = require("./alertsnews");
const { News } = require("./news");

const { Auth } = require("./auth");

module.exports = {
  MainNews,
  AlertsNews,
  News,
  Auth,
};
